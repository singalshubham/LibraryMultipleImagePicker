<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/activity_slide_image"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activity.SlideImageActivity">

  <android.support.v7.widget.Toolbar
        android:id="@+id/slide_image_activity_toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorPrimary"
        app:titleTextColor="@color/colorWhite"
        app:theme="@style/ToolbarColoredBackArrow" />
    <android.support.v4.view.ViewPager
        android:id="@+id/pager_slide_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/slide_image_activity_toolbar"/>
</RelativeLayout>
